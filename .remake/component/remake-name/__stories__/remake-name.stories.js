import React from 'react';

import { <%= name %> } from '../index';

export default {
	component: <%= name %>,
	title: 'Components/<%= name %>',
};

export const _default = () => {
	return <<%= name %> />;
};
