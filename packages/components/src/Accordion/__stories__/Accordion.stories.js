import React from 'react';

import { Accordion } from '../index';

export default {
	component: Accordion,
	title: 'Components/Accordion',
};

export const _default = () => {
	return <Accordion />;
};
