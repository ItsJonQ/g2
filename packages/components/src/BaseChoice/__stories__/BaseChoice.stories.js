import React from 'react';

import { BaseChoice } from '../index';

export default {
	component: BaseChoice,
	title: 'Components/BaseChoice',
};

export const _default = () => {
	return <BaseChoice />;
};
