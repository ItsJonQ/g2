import React from 'react';

import { ColorField } from '../index';

export default {
	component: ColorField,
	title: 'ColorField',
};

export const _default = () => {
	return <ColorField css={{ maxWidth: 60 }} />;
};
