import { styled, ui } from '@wp-g2/styles';

export const ColorSwatchView = styled.div`
	border-radius: ${ui.get('controlBorderRadius')};
	box-shadow: 0 0 0 1px rgba(0, 0, 0, 0.1) inset;
	height: 100%;
	width: 100%;
`;
