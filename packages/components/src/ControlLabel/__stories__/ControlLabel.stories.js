import React from 'react';

import { ControlLabel } from '../index';

export default {
	component: ControlLabel,
	title: 'ControlLabel',
};

export const _default = () => {
	return <ControlLabel />;
};
