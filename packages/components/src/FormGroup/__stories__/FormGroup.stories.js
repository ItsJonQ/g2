import React from 'react';

import { FormGroup } from '../index';

export default {
	component: FormGroup,
	title: 'Components/FormGroup',
};

export const _default = () => {
	return <FormGroup />;
};
