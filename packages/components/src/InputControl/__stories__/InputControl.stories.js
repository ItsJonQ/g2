import React from 'react';

import { InputControl } from '../index';

export default {
	title: 'InputControl',
	component: InputControl,
};

export const _default = () => {
	return (
		<div>
			<InputControl />
		</div>
	);
};
