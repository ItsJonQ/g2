import React from 'react';

import { InputControl } from '../index';

export default {
	component: InputControl,
	title: 'InputControl',
};

export const _default = () => {
	return <InputControl />;
};
