import React from 'react';

import { Separator } from '../index';

export default {
	component: Separator,
	title: 'Components/Separator',
};

export const _default = () => {
	return <Separator />;
};
