import React from 'react';

import { CardBody } from '../../../index';
import { Screen, ScreenHeader, SettingLink, StylePreview } from '../components';

export const TypographyScreen = () => {
	return (
		<Screen>
			<CardBody>
				<ScreenHeader back="/" title="Typography" />
			</CardBody>
		</Screen>
	);
};
