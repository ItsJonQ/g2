/** Retrieves the HTML selector for a System Component. */
export declare interface SelectorInterface {
	/** Retrieves the HTML selector for a System Component. */
	(ComponentName: string): string;
}
