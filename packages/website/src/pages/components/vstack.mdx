---
title: VStack
description: "VStack (or Vertical Stack) is a layout component that arranges child elements in a vertical line."
type: layout
---

# VStack

`VStack` (or Vertical Stack) is a layout component that arranges child elements in a vertical line.

### Usage

```jsx
import { VStack } from "@wp-g2/components"
```

`VStack` can render anything inside.

```jsx live
<VStack css={[ui.frame.height(200)]}>
  <View css={[ui.background.blue]}>
    <Text>Ana</Text>
  </View>
  <View css={[ui.background.blue]}>
    <Text>Elsa</Text>
  </View>
  <View css={[ui.background.blue]}>
    <Text>Olaf</Text>
  </View>
</VStack>
```

### Spacer

When a `Spacer` is used within an `VStack`, the `Spacer` adaptively expands to take up the remaining space.

```jsx live
<VStack css={[ui.frame.height(200)]}>
  <View css={[ui.background.blue]}>
    <Text>Ana</Text>
  </View>
  <Spacer css={[ui.background.blue]}>
    <Text>Elsa</Text>
  </Spacer>
  <View css={[ui.background.blue]}>
    <Text>Olaf</Text>
  </View>
</VStack>
```

`Spacer` can also be used in-between items to push them apart.

```jsx live
<VStack css={[ui.frame.height(200)]}>
  <View css={[ui.background.blue]}>
    <Text>Ana</Text>
  </View>
  <Spacer />
  <View css={[ui.background.blue]}>
    <Text>Elsa</Text>
  </View>
  <View css={[ui.background.blue]}>
    <Text>Olaf</Text>
  </View>
</VStack>
```

### Props

`VStack` uses [`Flex`](/components/Flex) underneath, so we have access to all of `Flex`'s props.

| Name      | Type     | Default | Description                                     |
| --------- | -------- | ------- | ----------------------------------------------- |
| alignment | `string` | `edge`  | Determines how the child elements are aligned.  |
| spacing   | `number` | `2`     | The amount of space between each child element. |

#### alignment

The alignment can be one of the following properties:

`top`, `topLeft`, `topRight`, `left`, `center`, `right`, `bottom`, `bottomLeft`, `bottomRight`, `edge`, `stretch`

```jsx live
<VStack alignment="center">
  <View css={[ui.background.blue]}>
    <Text>Ana</Text>
  </View>
  <Spacer />
  <View css={[ui.background.blue]}>
    <Text>Elsa</Text>
  </View>
  <View css={[ui.background.blue]}>
    <Text>Olaf</Text>
  </View>
</VStack>
```

#### spacing

Spacing in between each child can be adjusted by using `spacing`. The value of `spacing` works as a multiplier to the library's grid system (base of `4px`).

```jsx live
<VStack alignment="center" spacing={5}>
  <View css={[ui.background.blue]}>
    <Text>Ana</Text>
  </View>
  <View css={[ui.background.blue]}>
    <Text>Elsa</Text>
  </View>
  <View css={[ui.background.blue]}>
    <Text>Olaf</Text>
  </View>
</VStack>
```

## See Also

- [`HStack`](/components/hstack)
- [`Flex`](/components/flex)
- [`Spacer`](/components/spacer)
